# Runtime dependencies for the Pac-Man clone.
# Install everything with:  pip install -r requirements.txt

pygame>=2.5,<3.0   # graphics, input, audio and the main loop
numpy>=1.24        # used to synthesise the sound effects (optional but recommended)
